// Daily rountine print in for loop in reverse

const rountine =['weak up', 'Take breakfast', 'Go to versity', 'back home'];

for (let i =rountine.length-1; i>=0; i--){
  const element=rountine[i];
  console.log(element);
}